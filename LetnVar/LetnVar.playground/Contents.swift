//: Playground - noun: a place where people can play

import UIKit

var str = "Hello, playground"

/*ex1. 선생님 설명 */
let name = "Seongkwan"
var weigh = 100.0

/*내 설명 */
let maxHour = 24
let maxMinute = 59
var currentHour = 9
var currentMInnute = 40
//: let은 기본적으로 수정이 불가능한 상수를 만들고 var는 수정이 가능한 변수를 만든다.

currentHour = "31"
/* currentHour의 경우에는 변수이기는 하나, 처음에 type이 주어질때 int값으로 주어졌기 때문에 int 값이 아니라 float값을 넣는 것은 불가능하다.*/

let currentHourList = [12, 13, 14]
/* currentHourList의 경우에는 let으로 설정되었기 때문에 그 안에 새로운 내용을 집어 넣을 수 없다. let의 경우에는 변경하려고 시도할 시 컴파일 에러가 발생한다. */
var currentMinuteList = [1, 2, 3, 4]
/*반면 currentMinuteList의 경우에는 var로 설정되었기 때문에 그 안에 새로운 내용을 추가하거나 변경하는 것이 가능하다. */



let maxWeight:Int = 500;

//maximum weight of a elevator

var newcomerWeight:Int += arc4random_uniform(80) + 40

//newcomer's weight to a elevator



let windowSize = CGRect(x: 0, y:0, width:320, height:240)

var plusSize += 10

var changeButtonSize = CGRect(x: 100, y:150, width:50+plusSize, height:50+plusSize)

//setting window size and changeable button size


let truth = "But yet it moves"

//everlasting, I cannot change this value

var saying = "I'm angry now"

//changeable, I can change this value always

